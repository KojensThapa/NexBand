import type { WritingCriterionAlgorithm } from "./writing-evaluation.types";

/** Extension point for the future coherence and cohesion evaluator. */
export type CoherenceAlgorithm = WritingCriterionAlgorithm;
