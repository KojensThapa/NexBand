import type { WritingCriterionAlgorithm } from "./writing-evaluation.types";

/** Extension point for the future grammar accuracy and range evaluator. */
export type GrammarAlgorithm = WritingCriterionAlgorithm;
